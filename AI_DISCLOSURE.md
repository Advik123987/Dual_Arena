# AI Tool Usage Disclosure

Required by The Zerops Challenge rules. Keep this updated as you continue
building — log what was AI-generated vs. what you wrote/decided yourself.

## Initial scaffold — generated by Claude (Anthropic), <TODAY'S DATE>

**Claude-generated (this session):**
- Full initial file/folder structure for `api/` (FastAPI) and `gui/` (React+Vite)
- `zerops-project-import.yaml` and both services' `zerops.yaml`
- Backend: matchmaking queue, WebSocket duel-room lifecycle (timer, submit,
  finalize), Groq problem-generation call with 3-model fallback chain,
  constrained-format judge (mcq/short_answer/sandboxed code exec), AI
  commentary calls, SQLAlchemy models, leaderboard/queue REST endpoints
- Frontend: duel room UI, timer, commentary feed, leaderboard, queue join flow
- Verified locally: `npm run build` succeeds, `python -m py_compile` succeeds,
  FastAPI app imports and route table is correct — **not yet verified**:
  actual live duel run against a real Postgres/Valkey, or a real Zerops deploy

**Human-decided (you, the author):**
- Overall concept, architecture, and constraint set (given to Claude as the brief)
- Which scope trade-offs to accept (see README "Scope decisions flagged for judges")
- <fill in as you review/modify the generated code>

## Later sessions

<Add an entry per session/tool — e.g. "Antigravity — fixed X, added Y" —
so the final disclosure accurately reflects the whole build, not just this
scaffold.>
